{{ form_start(form) }}
    {{ form_widget(form) }}

<div class="row">
    <div class="col-2">
        <button class="btn cur-p btn-outline-info">{{ button_label|default('Save') }}</button>
    </div>

    {{ form_end(form) }}

    {% if button_delete | default(false) %}
        <div class="mr-auto">
            {{ include('student/_delete_form.html.twig') }}
        </div>
    {% endif %}
</div>


